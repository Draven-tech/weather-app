import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.yourname.weatherapp',
  appName: 'Zandale - Weather App',
  webDir: 'www',
};

export default config;
